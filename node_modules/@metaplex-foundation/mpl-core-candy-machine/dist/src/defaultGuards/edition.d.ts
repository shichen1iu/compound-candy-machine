import { Edition, EditionArgs } from '../generated';
import { GuardManifest } from '../guards';
/**
 * The edition guard is used add the edition plugin to minted assets
 */
export declare const editionGuardManifest: GuardManifest<EditionArgs, Edition, EditionMintArgs>;
export declare type EditionMintArgs = Omit<EditionArgs, 'lamports'>;
