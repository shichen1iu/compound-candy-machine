import { VanityMint, VanityMintArgs } from '../generated';
import { GuardManifest } from '../guards';
/**
 * The vanityMint guard verifies that the new asset
 * address matches the provided regex.
 */
export declare const vanityMintGuardManifest: GuardManifest<VanityMintArgs, VanityMint>;
